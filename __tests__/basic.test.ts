/**
 * Test básico para que el CI/CD pipeline pase
 * Puedes reemplazar esto con tests reales después
 */

describe('Basic Tests', () => {
  it('should pass a basic test', () => {
    expect(true).toBe(true);
  });

  it('should handle simple math', () => {
    expect(2 + 2).toBe(4);
  });
});
